# Copy to render.env, fill real values, then paste into Render:
# Dashboard → martisoor-api → Environment → Add from .env
#
# Do NOT commit render.env (it has secrets).

NODE_ENV=production
DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require
JWT_SECRET=change-me-to-a-long-random-string
